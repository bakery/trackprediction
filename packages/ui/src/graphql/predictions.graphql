query predictions($filters: PredictionFiltersInput, $sort: [String]) {
  predictions(filters: $filters, sort: $sort) {
    data {
      ...PredictionData
    }
    meta {
      ...CollectionMeta
    }
  }
}
